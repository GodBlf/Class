# CryptoBoot
- new()加密变量->writer写入字节数组->sum()返回加密结果
```go
func Md5(text string) string { //md5加密
	hash := md5.New()                 //生成加密变量
	hash.Write([]byte(text))          //写入加密变量
	digest := hash.Sum(nil)           //加密生成加密结果
	ans := hex.EncodeToString(digest) //字节数组转成字符串
	return ans
}
```