# 知识点
转义序列 \101 ='A' \x42='B'  0开头是8进制  0b二进制 0x16进制
截断八进制后三位换成2^3 十六进制后两位换成2^4,将原数转成二进制1变4(十六进制) 1变3(8进制) 然后char截断8位
else 就近原则,只有一条语句省略{}
while(i++) if(i++) 判断false后记得++
float a, b;
a = 6.7;
a ！= 6.7
int a[][3] = { 5,4,3,2,1 };2x3矩阵卧槽

## 优先级
1. ()
2. 所有单目包括! [] .
3. 四则运算
4. 逻辑 > >= 偏序 == != && ||
5. 三目 ? :
6. 赋值 注意+/等向右结合  +=x/=  先算/=
7. ,
记忆 1 2 3 赋值,

## switch
无break后边语句全部执行包括default
记得判断false 要执行default
## printf格式
![alt text](image.png)
d,i,o,x,X,u,c,s,f,e,E,g,G
ld lf
小数输出(默认保留6位)
010.2f

## string
- strlen(str) strcpy(dest,"") str3=strcat(str1,str2)
strcmp(str1,str2)

## 函数
#include <stdio.h>

// 1. 函数声明 (Function Prototype)
int max(int x, int y);

int main() {
}

// 3. 函数定义 (Function Definition)
int max(int x, int y) {
}

## 指针
int * arr=(int*) malloc(n*sizeof(int));

## 静态
static int j = 0;：静态变量只在程序开始时初始化一次。函数调用结束后，j 的值会保留，下次进入函数时使用上次留下的值。

## 结构体 union enum
需要加分号末尾
结构体一定要时刻带着 struct  typedef 取小名
共用体成员共用一个内存
![alt text](image-1.png)
1 2 3 4
b
c
d
e
# 期末


# 期中
## 2024
1. 短路
2. 转义序列,就近原则

## 2023
###
1. 优先级
2. 输出格式
3. 转成2进制位再截断
4. a<c||b-- b直接短路了
5. sd
6. swtich
8. 宏
9. char []="\n\0"都是转义字符 \x86也是
## 
define无分号
赋初值
1. 0 0 0 -50
2. 2.500000
    2
3. 2 4 2 0
4. 27 ,2 
5.  4 6 1 10
6.  abcdeedcba
sum=5,i=5,j=5

7. 1 -126
8. 1 7 2 5
9. 8 0 4 2 e _ a x
10. 14 7

